<span *ngIf="isAuthenticated()">
  <nav class="navbar navbar-expand bg-light navbar-light fixed-top">
    <div class="navbar-header">
      <!-- <a routerLink="/" class="navbar-brand"> temporarily disable till we have proper dashboard :)
        <img src="./assets/images/EZIVIEW.ICO" width="30" height="30" class="d-inline-block align-top" alt="">
        <b>Multi</b>Drive
      </a> -->
      <a class="navbar-brand">
        <img src="./assets/images/EZIVIEW.ICO" width="30" height="30" class="d-inline-block align-top" alt="">
        <b>Aurora</b>
      </a>
    </div>

    <div class="collapse navbar-collapse">
      <ul class="navbar-nav" routerLinkActive="active">
        <li class="nav-item"><a class="nav-link nav-link-grow-up" (click)="routeLink(1)"
          routerLinkActive="active" [class.disabled]="!disableClick ? true : null" >DEVICES</a></li>
        <li class="nav-item"><a class="nav-link nav-link-grow-up" (click)="routeLink(2)"
            routerLinkActive="active" [class.disabled]="!disableClick ? true : null" >SITES</a></li>
        <li class="nav-item"><a class="nav-link nav-link-grow-up" (click)="routeLink(3)"
            routerLinkActive="active" [class.disabled]="!disableClick ? true : null">KEYS</a></li>
        <li class="nav-item"><a class="nav-link nav-link-grow-up" (click)="routeLink(4)"
            routerLinkActive="active" [class.disabled]="!disableClick ? true : null">FIRMWARES</a></li>
        <!-- <li class="nav-item"><a class="nav-link nav-link-grow-up" [routerLink]="['/schedules']"
            routerLinkActive="active">SCHEDULES</a></li> -->
      </ul>
    </div>

    <button class="btn btn-danger" [disabled]="!disableClick" (click)="logout()">Logout {{ currentUser }} </button>

  </nav>
  <br>
</span>