import { HttpEventType } from '@angular/common/http';
import { Component, Input, OnInit } from '@angular/core';
import { SetupService } from 'src/app/devices/setup.service';

@Component({
  selector: 'app-multiple-setup',
  templateUrl: './multiple-setup.component.html',
  styleUrls: ['./multiple-setup.component.css']
})
export class MultipleSetupComponent implements OnInit {

  @Input() ids: any;
  percent: any;

  constructor(public setupService: SetupService) { }

  public ngOnInit() {
    console.log("Testing...");
    // Please remove this code snippets. This is research code.
    // this.setupService.getValues(this.ids).subscribe(
    //   event => {
    //     console.log(event.type);
    //     if (event.type === HttpEventType.DownloadProgress) {
    //       console.log("download progress");
    //       let percentDone = Math.round(100 * event.loaded / event.total);
    //       console.log("Percent.." + percentDone);
    //       this.percent = percentDone;
    //     }
    //     if (event.type === HttpEventType.Response) {
    //       console.log("donwload completed");
    //     }
    //   },
    //   (error: any) => {
    //     console.log(error);
    //   },
    //   () => {

    //   });
  }

  showPercent() {
    return this.percent;
  }

}
