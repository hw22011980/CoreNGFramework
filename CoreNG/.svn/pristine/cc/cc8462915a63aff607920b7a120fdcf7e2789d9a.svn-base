import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DevicesPerSiteComponent } from './devices-per-site.component';

describe('DevicesPerSiteComponent', () => {
  let component: DevicesPerSiteComponent;
  let fixture: ComponentFixture<DevicesPerSiteComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DevicesPerSiteComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DevicesPerSiteComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
