import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DeviceLinkComponent } from './device-link.component';

describe('DeviceLinkComponent', () => {
  let component: DeviceLinkComponent;
  let fixture: ComponentFixture<DeviceLinkComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DeviceLinkComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DeviceLinkComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
