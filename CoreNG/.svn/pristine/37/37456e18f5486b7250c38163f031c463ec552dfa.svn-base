import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { StageBasic } from './stage-basic.component';

describe('StageBasicComponent', () => {
  let component: StageBasic;
  let fixture: ComponentFixture<StageBasic>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ StageBasic ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(StageBasic);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
