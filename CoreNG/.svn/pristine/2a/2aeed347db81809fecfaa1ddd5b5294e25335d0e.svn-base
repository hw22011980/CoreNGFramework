import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { StandardResponseComponent } from './standard-response.component';

describe('StandardResponseComponent', () => {
  let component: StandardResponseComponent;
  let fixture: ComponentFixture<StandardResponseComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ StandardResponseComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(StandardResponseComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
