import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SchedulesInfoComponent } from './schedules-info.component';

describe('SchedulesInfoComponent', () => {
  let component: SchedulesInfoComponent;
  let fixture: ComponentFixture<SchedulesInfoComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SchedulesInfoComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SchedulesInfoComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
