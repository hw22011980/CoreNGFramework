import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DeviceNavigationComponent } from './device-navigation.component';

describe('DeviceNavigationComponent', () => {
  let component: DeviceNavigationComponent;
  let fixture: ComponentFixture<DeviceNavigationComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DeviceNavigationComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DeviceNavigationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
