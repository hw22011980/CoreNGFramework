import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ActionResponseComponent } from './action-response.component';

describe('ActionResponseComponent', () => {
  let component: ActionResponseComponent;
  let fixture: ComponentFixture<ActionResponseComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ActionResponseComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ActionResponseComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
